version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    restart: always
    environment:
      POSTGRES_DB: helloavatar
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres']
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: always
    environment:
      DATABASE_URL: postgresql://postgres:${DB_PASSWORD}@postgres:5432/helloavatar
      JWT_SECRET: ${JWT_SECRET}
      HEYGEN_API_KEY: ${HEYGEN_API_KEY}
      SIMLI_API_KEY: ${SIMLI_API_KEY}
      SIMLI_FACE_ID: ${SIMLI_FACE_ID}
      GROQ_API_KEY: ${GROQ_API_KEY}
      EDGE_TTS_VOICE: ${EDGE_TTS_VOICE:-en-US-JennyNeural}
      FRONTEND_URL: http://${DOMAIN}
      BACKEND_URL: http://${DOMAIN}/api
      PORT: 4000
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - uploads_data:/app/uploads

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_URL: http://${DOMAIN}/api
    restart: always
    environment:
      NODE_ENV: production
    depends_on:
      - backend

  nginx:
    image: nginx:alpine
    restart: always
    ports:
      - '8888:80'
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - backend
      - frontend

volumes:
  postgres_data:
  uploads_data:
